/* YMS admin monthly counseling tracker — teacher checklist sync */
(function(){
  'use strict';
  if(!location.pathname.endsWith('/admin.html')) return;
  let students=[],counseling=[],month=new Date().toISOString().slice(0,7),teacher='ALL',status='ALL',loading=false,lastLoadedAt=0;
  const LOAD_TTL=120000;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const section=()=>document.getElementById('section-counseling');
  function css(){if(document.getElementById('yms-admin-counseling-monthly-css'))return;const s=document.createElement('style');s.id='yms-admin-counseling-monthly-css';s.textContent=`.acm-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;flex-wrap:wrap;margin-bottom:16px}.acm-title{font-size:18px;font-weight:900;color:#14245A}.acm-sub{font-size:11px;color:#7A87A8;margin-top:4px}.acm-filters{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:14px}.acm-filters select,.acm-filters input{height:36px;border:1px solid #C8D1E8;border-radius:9px;background:#fff;padding:0 10px;font:inherit;font-size:12px;color:#273453}.acm-summary{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:14px}.acm-pill{background:#fff;border:1px solid #E1E6F0;border-radius:999px;padding:7px 12px;font-size:11px;color:#56637E}.acm-pill strong{color:#17284F}.acm-table-wrap{overflow:auto;border:1px solid #DCE3F0;border-radius:14px;background:#fff;max-height:68vh}.acm-table{width:100%;border-collapse:separate;border-spacing:0;min-width:980px;font-size:12px}.acm-table th{position:sticky;top:0;background:#1E4FD7;color:#fff;padding:10px;border-right:1px solid rgba(255,255,255,.2);text-align:center;z-index:2}.acm-table td{padding:10px;border-right:1px solid #E6EAF2;border-bottom:1px solid #E6EAF2;text-align:center;background:#fff}.acm-table th.name,.acm-table td.name{position:sticky;left:0}.acm-table th.name{z-index:4;background:#163CA9}.acm-table td.name{z-index:1;text-align:left;font-weight:800;color:#243356;box-shadow:2px 0 5px rgba(30,50,120,.06)}.acm-note{text-align:left!important;min-width:220px;max-width:360px;white-space:pre-wrap;line-height:1.45;color:#46516C}.acm-done{display:inline-flex;align-items:center;gap:4px;padding:5px 9px;border-radius:999px;background:#E8F6EF;color:#237A53;border:1px solid #A9D9C1;font-weight:800}.acm-pending{display:inline-flex;align-items:center;gap:4px;padding:5px 9px;border-radius:999px;background:#FFF3E0;color:#C56B00;border:1px solid #FFD49B;font-weight:800}.acm-empty{padding:30px;text-align:center;color:#8793AA}`;document.head.appendChild(s)}
  function monthOf(c){return String(c.month||c.repliedAt||c.updatedAt||c.createdAt||'').slice(0,7)}
  function completedRecord(studentId){const list=counseling.filter(c=>String(c.studentId||'')===String(studentId)&&String(c.status||'').toUpperCase()==='REPLIED'&&monthOf(c)===month);return list.sort((a,b)=>{const am=String(a.recordType||'')==='MONTHLY_TEACHER'?1:0,bm=String(b.recordType||'')==='MONTHLY_TEACHER'?1:0;if(am!==bm)return bm-am;return String(b.updatedAt||b.repliedAt||b.createdAt||'').localeCompare(String(a.updatedAt||a.repliedAt||a.createdAt||''));})[0]||null}
  const teacherNameFor=(s,r)=>r?.teacherName||s.teacherName||'-';
  const activeStudents=()=>students.filter(s=>s&&s.isActive!==false&&String(s.name||'').trim());
  function filteredRows(){return activeStudents().map(s=>{const rec=completedRecord(s.id);return{s,rec,done:!!rec,teacher:teacherNameFor(s,rec)}}).filter(x=>teacher==='ALL'||x.teacher===teacher).filter(x=>status==='ALL'||(status==='DONE'?x.done:!x.done)).sort((a,b)=>String(a.s.name||'').localeCompare(String(b.s.name||''),'ko'))}
  function render(){const el=section();if(!el)return;const active=activeStudents(),rows=filteredRows(),mapped=active.map(s=>({s,rec:completedRecord(s.id)})),done=mapped.filter(x=>x.rec).length,pending=active.length-done;const teachers=[...new Set([...active.map(s=>s.teacherName),...counseling.filter(c=>monthOf(c)===month).map(c=>c.teacherName)].filter(Boolean))].sort((a,b)=>a.localeCompare(b,'ko'));el.innerHTML=`<div class="acm-head"><div><div class="acm-title">상담 관리</div><div class="acm-sub">등록 학생 전체를 기준으로 선생님의 월 상담 완료 여부와 특이사항을 확인합니다.</div></div></div><div class="acm-filters"><span style="font-size:12px;color:#5C6880">월</span><input type="month" id="acmMonth" value="${esc(month)}"><span style="font-size:12px;color:#5C6880">선생님</span><select id="acmTeacher"><option value="ALL">전체</option>${teachers.map(t=>`<option value="${esc(t)}" ${teacher===t?'selected':''}>${esc(t)}</option>`).join('')}</select><span style="font-size:12px;color:#5C6880">상태</span><select id="acmStatus"><option value="ALL" ${status==='ALL'?'selected':''}>전체</option><option value="DONE" ${status==='DONE'?'selected':''}>완료</option><option value="PENDING" ${status==='PENDING'?'selected':''}>미완료</option></select></div><div class="acm-summary"><span class="acm-pill">등록 학생 <strong>${active.length}명</strong></span><span class="acm-pill">상담 완료 <strong>${done}명</strong></span><span class="acm-pill">미완료 <strong>${pending}명</strong></span><span class="acm-pill">완료율 <strong>${active.length?Math.round(done/active.length*100):0}%</strong></span></div><div class="acm-table-wrap"><table class="acm-table"><thead><tr><th class="name">학생</th><th>학교</th><th>학년</th><th>반</th><th>담당 선생님</th><th>${esc(month.replace('-','년 '))}월 상담</th><th>상담일</th><th>특이사항</th></tr></thead><tbody>${rows.length?rows.map(x=>{const note=x.rec?.specialNote||x.rec?.notes||x.rec?.reply||'';return`<tr><td class="name">${esc(x.s.name||'-')}</td><td>${esc(x.s.schoolName||'-')}</td><td>${esc(x.s.grade||'-')}</td><td>${esc(x.s.className||'-')}</td><td>${esc(x.teacher||'-')}</td><td>${x.done?'<span class="acm-done">✓ 완료</span>':'<span class="acm-pending">○ 미완료</span>'}</td><td>${x.rec?esc(String(x.rec.repliedAt||x.rec.updatedAt||x.rec.createdAt||'').slice(0,10)):'-'}</td><td class="acm-note">${note?esc(note):'-'}</td></tr>`}).join(''):`<tr><td colspan="8" class="acm-empty">등록된 학생이 없습니다</td></tr>`}</tbody></table></div>`;document.getElementById('acmMonth').onchange=e=>{month=e.target.value||new Date().toISOString().slice(0,7);render()};document.getElementById('acmTeacher').onchange=e=>{teacher=e.target.value;render()};document.getElementById('acmStatus').onchange=e=>{status=e.target.value;render()}}
  async function load(force=false){const now=Date.now();if(loading)return;if(!force&&lastLoadedAt&&now-lastLoadedAt<LOAD_TTL){render();return;}loading=true;const el=section();if(!el){loading=false;return}try{let studentSource=(typeof _allStudents!=='undefined'&&Array.isArray(_allStudents)&&_allStudents.length)?[..._allStudents]:null;const tasks=[_tFetch('tables/counseling?limit=1000')];if(!studentSource)tasks.unshift(_tFetch('tables/students?limit=1000'));const rs=await Promise.all(tasks);let cr;if(studentSource){cr=rs[0];students=studentSource;}else{const sr=rs[0];cr=rs[1];if(sr.ok)students=(await sr.json()).data||[];}counseling=cr&&cr.ok?((await cr.json()).data||[]):[];students=students.filter(Boolean);lastLoadedAt=Date.now();render()}catch(e){console.error('[YMS] admin counseling monthly',e);if(typeof _allStudents!=='undefined'&&Array.isArray(_allStudents)){students=[..._allStudents];render()}}finally{loading=false}}
  function scheduleLoad(){teacher='ALL';status='ALL';setTimeout(()=>load(false),80)}
  function install(){css();const old=window.switchSection;if(typeof old==='function'&&!old.__acmWrapped){const f=function(name,el){const r=old.apply(this,arguments);if(name==='counseling')scheduleLoad();return r};f.__acmWrapped=true;window.switchSection=f}document.addEventListener('click',e=>{if(e.target.closest('#nav-counseling'))scheduleLoad()},true);if(section()&&!section().classList.contains('hidden'))load(false)}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();